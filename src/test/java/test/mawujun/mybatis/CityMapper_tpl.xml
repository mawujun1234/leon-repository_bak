<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="test.mawujun.repository.CityMapper">
	<sql id="BaseColumns">
		id as id,name as name,age as age,price as price,create_date as createDate,sex as sex
	</sql>
	<sql id="WhereColumns">
		<where>
			<if test="id != null">
				id = #{id}
			</if>
			<if test=" name!= null">
				and name= #{name}
			</if>
			<if test=" age!= null">
				and age= #{age}
			</if>
			<if test=" price!= null">
				and price= #{price}
			</if>
			<if test=" createDate!= null">
				and create_date= #{createDate}
			</if>
			<if test=" sex!= null">
				and sex= #{sex}
			</if>
		</where>
	</sql>
	
    <!-- START::: 系统自动生成，请勿修改  -->
    <select id="getById" resultType="city" parameterType="java.lang.String">
        select
		<include refid = "BaseColumns" />
		from t_city where id = #{id}
    </select>
    
    <select id="listByMap" resultType="city" parameterType="java.util.Map">
        select
		<include refid="test.mawujun.repository.BaseCityMapper.BaseColumns" />
		from t_city
		<include refid="test.mawujun.repository.BaseCityMapper.WhereColumns" />
    </select>
 
    
    <select id="getMapById" resultType="beanmap" parameterType="java.lang.String">
       select
		<include refid = "BaseColumns" />
		from t_city where id = #{id}
    </select>

    <select id="listByExample" resultType="city" parameterType="city">
        select
		<include refid="test.mawujun.repository.BaseCityMapper.BaseColumns" />
		from t_city
		<include refid="test.mawujun.repository.BaseCityMapper.WhereColumns" />
    </select>

    <select id="listPageByMap" resultType="city" >
        select
		<include refid="test.mawujun.repository.BaseCityMapper.BaseColumns" />
		from t_city
		<include refid="test.mawujun.repository.BaseCityMapper.WhereColumns" />
    </select>

    <select id="countByMap" resultType="int" parameterType="java.util.Map">
        select count(id) from t_city
		<include refid="test.mawujun.repository.BaseCityMapper.WhereColumns" />
    </select>
    <select id="countByExample" resultType="int" parameterType="city">
        select count(id) from t_city
		<include refid="test.mawujun.repository.BaseCityMapper.WhereColumns" />
    </select>

    <insert id="create" keyProperty="id" parameterType="city">
        <selectKey resultType="java.lang.String" order="BEFORE" keyProperty="id">
            select IFNULL(#{id}, UUID());
        </selectKey> 
        <include refid="test.mawujun.repository.BaseCityMapper.create"></include>
    </insert>
    
    <insert id="createBatch" keyProperty="id" parameterType="list"> 
    	INSERT into t_city(
			<include refid="test.mawujun.repository.BaseCityMapper.BaseFields" />
		)
		values
		<foreach collection="list" item="item" index="index" separator=",">
		 	( IFNULL(#{item.id}, UUID()),#{item.name},#{item.age},#{item.price},#{item.createDate},#{item.sex})
		</foreach>
    </insert>

    <update id="update" parameterType="city">
        UPDATE t_city
		<set>
			<if test=" name!= null">name= #{name}, </if>
			<if test=" age!= null"> age= #{age}, </if>
			<if test=" price!= null">price= #{price}, </if>
			<if test=" createDate!= null"> create_date = #{createDate}, </if>
			<if test=" sex!= null">sex= #{sex}, </if>
		</set>
		WHERE id = #{id}
    </update>
    <update id="updateBatch" parameterType="list">
	    <foreach collection="list" item="item" index="index" separator=";">
	    	<include refid="test.mawujun.repository.BaseCityMapper.update"></include>
	    </foreach>
    </update>


    <delete id="remove" parameterType="city">
        DELETE from t_city	 
		<if test="_parameter==null">
			WHERE 1=2
		</if>
		<include refid="test.mawujun.repository.BaseCityMapper.WhereColumns" />
    </delete>

    <delete id="removeById" parameterType="java.lang.String">
        <include refid="test.mawujun.repository.BaseCityMapper.removeById"></include>
    </delete>

    <delete id="removeByIds">
        DELETE from t_city WHERE id in
		<foreach collection="array" index="index" item="item" open="(" separator="," close=")">
			#{item}
		</foreach>
    </delete>

    

</mapper>

 